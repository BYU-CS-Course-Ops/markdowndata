---
name: markdowndata-reference
description: Reference documentation for markdowndata syntax and Python API
---

# markdowndata Reference

Choose the reference you need:

- **`syntax`** — Complete reference for all value types (YAML dict, MD table, MD list, MD text, headless variants)
- **`api`** — Python API: how to call `loads()` and `load()`, import, access patterns
